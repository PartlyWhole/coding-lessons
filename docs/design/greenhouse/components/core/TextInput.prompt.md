Answer input for predict/recall steps; mono because the learner types program output. Focus = accent border + tint halo (via trellis-ui.css when inside .cell-runner).

```jsx
<TextInput placeholder="Type exactly what gets printed…" />
<TextInput width={200} defaultValue="int" />
```
