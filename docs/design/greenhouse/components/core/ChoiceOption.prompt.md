Whole-row radio choice card. Selected = Bluebell border + tint fill + filled radio; disabled rows dim to 55% but stay readable (learners review what they picked).

```jsx
<ChoiceOption name="q1" checked>Hi Alan</ChoiceOption>
<ChoiceOption name="q1" code>name = input("What's your name? ")</ChoiceOption>
```
