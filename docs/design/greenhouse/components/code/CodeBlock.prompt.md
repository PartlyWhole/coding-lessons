Dark terrarium code block with optional docked stdout strip ("what came out the bottom").

```jsx
<CodeBlock output="This is cool!">{'print("This " + "is" + " cool!")'}</CodeBlock>
```

Token colors: wrap spans with color:var(--code-keyword|string|number|builtin|comment).
