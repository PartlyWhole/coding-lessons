{
  "name": "@trellis/integration-tests",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "lint": "eslint test --ext .ts"
  },
  "devDependencies": {
    "@trellis/authoring": "workspace:*",
    "@trellis/engine": "workspace:*",
    "@trellis/sandbox": "workspace:*",
    "@trellis/schema": "workspace:*",
    "@types/node": "^24.0.0",
    "vitest": "^2.1.0"
  }
}
